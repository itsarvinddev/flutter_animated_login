def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader ->
        localProperties.load(reader)
    }
}

def flutterRoot = localProperties.getProperty('flutter.sdk')
if (flutterRoot == null) {
    throw new GradleException("Flutter SDK not found. Define location with flutter.sdk in the local.properties file.")
}

def flutterVersionCode = localProperties.getProperty('flutter.versionCode')
if (flutterVersionCode == null) {
    flutterVersionCode = '1'
}

def flutterMinSdkVersion = localProperties.getProperty('flutter.minSdkVersion')
if (flutterMinSdkVersion == null) {
    flutterMinSdkVersion = '23'
}

def flutterTargetSdkVersion = localProperties.getProperty('flutter.targetSdkVersion')
if (flutterTargetSdkVersion == null) {
    flutterTargetSdkVersion = '33'
}
def flutterCompileSdkVersion = localProperties.getProperty('flutter.compileSdkVersion')
if (flutterCompileSdkVersion == null) {
    flutterCompileSdkVersion = '34'
}

def flutterVersionName = localProperties.getProperty('flutter.versionName')
if (flutterVersionName == null) {
    flutterVersionName = '1.0'
}

apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"

android {
    compileSdkVersion flutterCompileSdkVersion.toInteger()
    ndkVersion '25.1.8937393'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    defaultConfig {
        applicationId "com.example.flutteranimatedlogin"
        minSdkVersion flutterMinSdkVersion.toInteger()
        targetSdkVersion flutterTargetSdkVersion.toInteger()
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
        multiDexEnabled true
    }

    signingConfigs {
        release {
            keyAlias System.getenv('APP_RELEASE') ? keystoreProperties['keyAlias'] : null
            keyPassword System.getenv('APP_RELEASE') ? keystoreProperties['keyPassword'] : null
            storeFile System.getenv('APP_RELEASE') ? file(keystoreProperties['storeFile']) : null
            storePassword System.getenv('APP_RELEASE') ? keystoreProperties['storePassword'] : null
        }
        debug {
        }
    }

    buildTypes {
        release {
            signingConfig System.getenv('APP_RELEASE') ? signingConfigs.release : signingConfigs.debug
            shrinkResources false
            minifyEnabled false
            multiDexEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.debug
            shrinkResources false
            minifyEnabled false
            multiDexEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

flutter {
    source '../..'
}

dependencies {
    // Intentionally empty. flutter_animated_login is pure Dart: it declares no
    // Flutter plugin and ships no native code, so the example needs nothing
    // here.
    //
    // This block used to pin org.jetbrains.kotlin:kotlin-stdlib-jdk7, which is
    // the legacy artifact behind the "Duplicate class kotlin.collections.jdk8"
    // build failure people hit when it is mixed with a modern kotlin-stdlib.
    // The Kotlin Gradle plugin supplies the standard library already.
    //
    // It also declared Firebase Messaging and Analytics, plus the
    // google-services *plugin* as a runtime dependency, with no
    // google-services.json anywhere in the project - all leftovers from an
    // unrelated app.
}
